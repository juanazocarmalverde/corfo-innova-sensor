# Django
Django>=3.2,<5.0  

# Django REST Framework
djangorestframework>=3.12,<4.0

# dj-rest-auth para autenticación
dj-rest-auth>=2.1.9,<3.0

# django-allauth para soporte de autenticación con dj-rest-auth
django-allauth>=0.44,<0.50

# Django REST Framework Token Authentication (si estás usando Token Authentication)
djangorestframework-simplejwt>=5.0,<6.0

# Python Requests para dj-rest-auth
requests>=2.26

# CORS Headers (si necesitas CORS en tu API)
django-cors-headers>=3.7,<4.0

# Otros paquetes que podrías necesitar (opcional)
# django-environ para manejar variables de entorno
django-environ>=0.8,<1.0

# Pillow para manejar imágenes si usas ImageField en tus modelos
Pillow>=8.0,<10.0
